'use strict';

// Path to moving mouse script
const scriptPath = "C:/Workspace/NodeEs6Test/commands/moveMouse.ps1";

let _spawn = require("child_process").spawn,child;

class Mouse {
	constructor(){
	}
}